function characterSequence2(input) {
    let text = input[0];
    let length = text.length; // показва дължината на текста!

    let letter = text[3] // взима третия символ !!
    console.log(length)



    for (let i = 0; i < text.length; i++) {
        let letter = text[i]
        console.log(letter)
    }
}
characterSequence2(["softuni"])