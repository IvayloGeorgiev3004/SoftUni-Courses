function textTest(input){
    let name = input[0]
    let index = 5
  let firstLetter = name.charAt(0)
  let sixthLetter = name.charAt(index)
  let secondLetter = name[1]
  console.log(name)
}
textTest(["Ivaylo    "])